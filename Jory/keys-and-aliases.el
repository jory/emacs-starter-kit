;; Keys and generic aliases
(global-set-key (kbd "C-c /") 'comment-or-uncomment-region)
(global-set-key (kbd "C-c o") 'occur)
(global-set-key (kbd "C-c %") 'query-replace-regexp)

(defalias 'qrr 'query-replace-regexp)
